# TigerBase

TigerBase is a mission control dashboard which provides near realtime overview of a users Cloudflare worker projects. Inspired by old mission control panels from old movies.

## Usage

Provide Cloudflare API token as environment variable to API and that's it

## How it works

TigerBase auto discovers all workers, durable objects, etc in your account and 